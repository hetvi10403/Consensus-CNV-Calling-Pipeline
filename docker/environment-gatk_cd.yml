name: base
channels:
  - conda-forge
  - bioconda
dependencies:
  - samtools=1.20
  - cnvpytor
  - hmmcopy       # provides the readCounter binary
  - r-ichorcna
  - pandas
  - git           # needed once, to pull ichorCNA's scripts/ directory
